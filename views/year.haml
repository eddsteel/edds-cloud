%section.contents
  %ul
    -(1..4).each do |month|
      %li
        %a{:href=>rurl("/#{year}/#{month}"),
           :title=>"#{month_name(month)}, #{year}"}
          =month_name(month)
  %ul
    -(5..8).each do |month|
      %li
        %a{:href=>rurl("/#{year}/#{month}"),
           :title=>"#{month_name(month)}, #{year}"}
          =month_name(month)
  %ul
    -(9..12).each do |month|
      %li
        %a{:href=>rurl("/#{year}/#{month}"),
           :title=>"#{month_name(month)}, #{year}"}
          =month_name(month)
